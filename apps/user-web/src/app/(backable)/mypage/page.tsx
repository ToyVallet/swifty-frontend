import { Main } from '@components/common';
import { UserHeader } from '@components/mypage';

export default function MyPage() {
  return (
    <Main>
      <UserHeader username="사용자" />
    </Main>
  );
}
